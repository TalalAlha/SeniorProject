# Brute-force / account lockout protection
django-axes>=7.0.0,<8.0

# Core Django and DRF
# Pinned to 5.2 LTS — covers CVE-2024-27351 / 39329 / 41989 / 42005 / 45230 /
# 45231 / 53907 and the 2025 advisories (32873, 48432, 57833).
Django>=5.2.7,<5.3
# 3.15.2 patches CVE-2024-21520 (XSS in HTML renderer).
djangorestframework>=3.15.2,<4.0

# JWT Authentication
# 5.3.1 patches CVE-2024-22513 (refresh-token survives password change).
djangorestframework-simplejwt>=5.3.1,<6.0

# CORS headers for React frontend
# 4.4.0 patches a dev-mode log-injection issue.
django-cors-headers>=4.4.0,<5.0

# Environment variables
python-decouple>=3.8

# Database — psycopg2-binary required for PostgreSQL in production.
# dj-database-url parses DATABASE_URL env var so the same code works
# locally on SQLite (no env var) and in production on PostgreSQL.
psycopg2-binary>=2.9.9,<3.0
dj-database-url>=2.1.0,<3.0

# Production server + static file serving
gunicorn>=21.2.0,<23.0
whitenoise>=6.7.0,<7.0

# PyTorch CPU-only build for the LSTM email generator (~200MB vs ~2GB CUDA build).
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0,<3.0

# Image handling (optional for now - can add later if needed)
# Pillow>=10.0

# API documentation
# drf-yasg is feature-frozen; consider migrating to drf-spectacular.
drf-yasg>=1.21.7,<2.0

# Utilities
python-dateutil>=2.9

# For data validation
validators>=0.22

# Email / SendGrid (uses Django SMTP backend — no extra SDK needed)
# sendgrid>=6.0  # Optional: only needed if using the SendGrid Web API directly

# Testing
pytest==8.3.3
pytest-django==4.9.0
coverage==7.6.4
locust==2.32.4
