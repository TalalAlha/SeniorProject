"""
Analytics models.
Placeholder — analytics data is derived from existing simulation, training, and campaign
models via aggregation queries in views.py rather than dedicated database tables.
Part of the 'analytics' app.
"""
from django.db import models
