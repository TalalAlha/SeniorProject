"""
Core views.
Placeholder — the 'core' app does not expose its own API views; it provides
shared utilities and permissions consumed by other apps.
Part of the 'core' app.
"""
from django.shortcuts import render
