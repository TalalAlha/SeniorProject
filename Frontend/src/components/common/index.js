/**
 * components/common/index.js — Public surface for the common UI component library.
 * Re-exports Button, Input, and LoadingSpinner for a single import path.
 */
export { default as Button } from './Button';
export { default as Input } from './Input';
export { default as LoadingSpinner } from './LoadingSpinner';
